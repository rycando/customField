//@ts-ignore

export interface Repository<T> {
// 	findOne(id?: string, option?: any, ...): Promise<T | null>;

// 	find(option?: any, ...): Promise<T[] | null>;

// 	findOneAndUpdate(id?: string, option?: any, ...): Promise<T | null>;

// 	findOneAndDelete(id?: string, option?: any, ...): Promise<T | null>;
// }
